---
import Navbar from "../components/Navbar.astro";
import "../../ProgresCSS.css";

interface Props {
  title: string;
}

const { title } = Astro.props;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="description" content="ProgresCSS is a CSS template." />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
    <link rel="stylesheet" href="../../ProgresCSS.css" />
  </head>
  <body>
    <Navbar />
    <main>
      <slot />
    </main>
  </body>
</html>

<style is:global>
  main {
    display: flex;
    flex-direction: column;
    gap: 1rem;
    margin: 50px 0;
    max-width: 600px;
  }

  article {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }
</style>
